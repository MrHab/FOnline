# Серверные файлы

## Production entry point

`server.js` — единственная production-точка входа. Версия берётся из
`package.json`; `/health` возвращает `name`, `version`, `uptimeSec`, число
игроков, число активных `locationRealities`, число пользователей и персонажей,
а также runtime-метрики (см. [«Наблюдение»](SERVER_RUNTIME.md#наблюдение)).
Поле `playerLimitPerLocation` равно `null`.

В `server.js` находятся:

- Express, CORS, раздача статики и `/health`;
- REST API auth и персонажей;
- загрузка и нормализация авторских данных;
- Socket.IO и блокировки активного аккаунта/персонажа;
- комнаты, движение, бой, прогрессия и серверный инвентарь;
- NPC, AI, лут, контейнеры, ресурсы, торговля и крафт;
- локальные/глобальные переходы и связь с живой пустошью;
- интервалы симуляции, housekeeping и snapshots.

Файл большой, поэтому новые независимые алгоритмы следует выносить в
`src/server/`, сохраняя `server.js` как слой интеграции.

## Активные модули `src/server/`

Импорты находятся в начале `server.js` и внутри самих модулей (подмодули
`wasteland-sim.js`, `world-economy.js`, артефактов и территории). Модули не
обращаются к Socket.IO и не заводят собственных интервалов: такты, время и
случайность им передаёт `server.js`; единственный таймер — отложенная запись в
`coalesced-writer.js`.

### Аккаунты, доступ и запись

| Файл | Назначение |
|---|---|
| `auth-rate-limit.js` | ограничение частоты auth-запросов: бюджеты на IP и на пару IP + identity, доверенный loopback-прокси |
| `password-hashing.js` | асинхронная PBKDF2-очередь хеширования паролей |
| `password-reset.js` | одноразовый reset token, TTL и содержимое письма |
| `guest-auth.js` | гостевые профили по SHA-256 отпечатку `deviceId` |
| `dev-access.js` | startup-политика и middleware для закрытого dev API |
| `kromka-state-store.js` | транзакционная запись `users.json` и `saves.json` через журнал с восстановлением при старте |
| `coalesced-writer.js` | single-flight таймер объединения частых JSON-записей |
| `webgl-delivery.js` | заголовки доставки Unity WebGL: кодек предсжатых файлов, `Content-Type`, `Vary` |

### Персонаж, предметы и задания

| Файл | Назначение |
|---|---|
| `kromka-character-progression.js` | нормализация единого каталога 7/16/41, строгая проверка создания и alias быстрого старта |
| `starting-loadout.js` | наборы нового персонажа: пустой старт обучения, припасы учебного двора, быстрый старт |
| `kromka-onboarding.js` | состояние обучения и первой миссии: факты действий, шаги, пропуск |
| `kromka-caravan-cinematic.js` | удержание приватной комнаты засады каравана на время заставки |
| `kromka-save-migration.js` | перенос сохранения в ревизию мира `kromka-1`: безопасная точка по узлам карты |
| `kromka-items.js` | каталог предметов и полевых рецептов: нормализация, индексы, публичная версия |
| `kromka-inventory-transactions.js` | журнал инвентарных операций по `requestId`: повтор без второй мутации |
| `kromka-critical-actions.js` | журнал критических социальных действий по `requestId` |
| `kromka-quests.js` | состояние квестов «Кромки»: старт, прогресс целей по событиям, выбор исхода, сдача, повторяемые награды |
| `kromka-faction-contracts.js` | id шести фракций, alias прежних id, репутация и временные контракты |
| `equipment-hands.js` | правила слотов рук (weapon/offhand) и двуручного оружия |

### Комнаты, бой и NPC

| Файл | Назначение |
|---|---|
| `room-lifecycle.js` | жизненный цикл эфемерных комнат встреч и мировых точек |
| `room-actor-spatial-index.js` | uniform-grid broad phase для коллизий плотной комнаты |
| `static-collision-spatial-index.js` | индекс authored static blockers для движения, LOS и A* |
| `model-colliders.js` | загрузка actor collider-каталога для радиусов персонажей и существ |
| `actor-facing.js` | передние оси моделей акторов, намерение и yaw поворота |
| `enemy-ai.js` | серверный ИИ NPC: восприятие, угроза, телеграф атаки, координация ближнего боя |
| `enemy-pathing.js` | поиск пути врага по сетке тайлов (A*, правило углов, притяжение к проходимому тайлу); сетку комнаты даёт вызывающий |
| `combat-critical.js` | правила критических попаданий |
| `kromka-combat-contract.js` | восемь типов урона и единый порядок крит → порог → броня → сопротивление для PvE/PvP |
| `kromka-death-loot.js` | политики потерь по режиму зоны, сохранение состояния «ранен» и идемпотентная транзакция выпадения |
| `zone-rules.js` | режимы зон: нормализация id и alias, PvP, потери, подписи |
| `npc-inventory.js` | фракционные запасы, доктрины экипировки, инвентарь NPC, торговый и трупный лут |
| `npc-routines.js` | постоянное поведение роли NPC без суточных окон; прерывания боем, тревогой и диалогом |
| `npc-smart-objects.js` | каталог слотов занятий (smart-объектов) и их резервирование за NPC |

### Аномалии и артефакты

| Файл | Назначение |
|---|---|
| `anomalies.js` | поля Искажений в комнатах: урон по пребыванию, бросок болта и разрядка |
| `shift-cycle.js` | цикл Сдвига по реальному времени (calm → warning → active → afterglow) и `hash32` |
| `artifact-instances.js` | экземпляры артефактов: тир и seed, детерминированные свойства, стабилизация, разбор, публичная запись без скрытых свойств |
| `artifact-effects.js` | установленные в пояс артефакты и сложение их числовых эффектов |
| `artifact-runtime.js` | серверные состояния поддержки: гидратация, стимулятор, намокание, оглушение, шум шагов |
| `artifact-spawns.js` | находки артефактов комнаты по Сдвигу, профиль детектора, подбор |
| `anomaly-artifact-births.js` | рождение артефактов аномалиями между Сдвигами; состояние полей хранится в `saves.json` |

### Глобальная карта и живая пустошь

| Файл | Назначение |
|---|---|
| `wasteland-sim.js` | глобальная симуляция: точки, патрули и подкрепления, контроль, конфликты, мировые задания, world zones |
| `wasteland-anomaly-cycle.js` | детерминированные районы Сдвига, защищённые последствия и причинные возможности артефактов |
| `wasteland-settlements.js` | запись о жизни поселения: нормализация при загрузке и публичный вид (почасового расчёта нет) |
| `wasteland-population.js` | население поселения: нормализация числа жителей и защищённого минимума |
| `wasteland-refugees.js` | разбор групп беженцев из старых сохранений и варианты локальной сцены поселения |
| `wasteland-logistics.js` | журнал грузов: закрывает грузы караванов из старых сохранений |
| `wasteland-party-speed.js` | чистые профили и пределы скорости мировых отрядов |
| `wasteland-party-membership.js` | нормализация состава отрядов, лимиты, patrol duty и сверка с персонажами |
| `wasteland-sim-utils.js` | нормализация значений, устойчивый PRNG, атомарное хранение состояния |
| `wasteland-factions.js` | нормализация фракций, столицы и инварианты защищённых столичных локаций |
| `wasteland-map-geometry.js` | масштаб карты, центры клеток, расстояния и радиусы входа |
| `wasteland-district-sites.js` | детерминированные районные точки интереса и охранные зоны столиц |
| `authored-world-sites.js` | авторское размещение точек мира из Unity: привязка точки к узлу карты, архив неразмещённых процедурных точек |
| `wasteland-localization.js` | замена англоязычных названий в сохранённом состоянии симуляции |
| `wasteland-world-tasks.js` | схема мировых заданий и операций отрядов (фазы патруля и снабжения) |
| `wasteland-site-instances.js` | устойчивые ID, seed и профили точек глобальной карты |
| `wasteland-stockpile.js` | чистые операции над складом и грузом |
| `wasteland-live-regions.js` | живые регионы, приоритетные события и aftermath-последствия |
| `faction-economy.js` | нормализация рецептов производства, торговых профилей и розничных рынков |
| `world-contracts.js` | семантический ключ мирового контракта и удаление дублей активных контрактов |
| `global-infrastructure.js` | дороги/трубопроводы, проверка суши и поиск маршрутов |
| `global-map-merge.js` | подмешивание авторской глобальной карты из поставки в runtime-карту |
| `global-exit-direction.js` | направление выхода из локальной карты на глобальную |
| `global-arrival-transition.js` | билет подтверждённого прибытия `pendingLocationTransition` (в снимке карты — `pendingWorldDrop`), TTL 90 с |
| `location-release.js` | список локаций публичной карты; приватные, встречные и осадные экземпляры в него не входят |

### Экономика v3 и опасные земли

| Файл | Назначение |
|---|---|
| `world-economy.js` | экономика v3: флаги `worldModel`, лестница зон, множители добычи и останки NPC |
| `loot-perks.js` | перки поиска: «Редкая находка» (останки NPC), «Нюх на тайники» (материалы тайника), «Падальщик» (трофеи существ) |
| `faction-market.js` | книга ордеров: встречные ордера на продажу и выкуп, сбор, налог, полки |
| `city-auctions.js` | книги ордеров столиц: налог продавца, переезд общей книги, полки |
| `black-market.js` | Чёрный рынок: полосы ценности, цена скупки, казна, склад, выпадение с NPC, распад и усталость фарма |
| `crafting-plots.js` | участки станков: торги за аренду, плата арендатору и поселению, возврат материалов |
| `account-sin.js` | синь на счёте аккаунта: журнал, премиум, фокус |
| `danger-cells.js` | опасные клетки карты: цвет по правилам, мелкие клетки 1,6 км, шанс и сцена стычки |
| `danger-ecology.js` | A-Life опасных клеток: логова, группы монстров и налётчиков, жизнь без игроков, чутьё, приход в сцены, потери и бегство |
| `zone-builder.js` | конструктор зон: из рецепта и каталога кусков собирает обычную локацию 160×160 тайлов (ворота, входы, тропы, объекты, тайники, аномалии, якоря A-Life, граф путей); чистый и детерминированный, правила под версией `BUILDER_VERSION` |
| `zone-graph.js` | граф зон мира: сетка 19×15 зон по 20 км из клеток карты, места, дороги и общие стороны зон; «зона в точке», сосед, путь по открытым воротам, рецепт зоны для конструктора |
| `zone-channels.js` | каналы зон мира (`z_CC_RR#chN` сверх 40 игроков), выбор канала для входящего и срок сна пустой зоны |
| `zone-runtime.js` | зоны на сервере: заглушка каждой зоны в `LOCATIONS` при старте, полное определение конструктором при первом входе (или закреплённое `data/zones/authored/<id>.json`), вид `self.zone` для клиента |
| `zone-chunks.js` | каталог конструктора: набор префабов `data/zones/kit.json`, куски `data/zones/chunks/*.json`, их проверка и повороты |

### Сердцевина, области и события

| Файл | Назначение |
|---|---|
| `territory-membership.js` | принадлежность персонажа к фракции Сердцевины, кулдаун смены, доступ к локациям территории |
| `territory-contract.js` | контракт наёмника у ворот Сердцевины: доли фракций и база выбранной фракции |
| `territory-outposts.js` | захватываемые аванпосты: удержание, блокировка после смены владельца, гарнизон |
| `lab-mechanics.js` | угрозы боковых лабораторий: шкала, удар по секторам зала, узлы сброса |
| `world-boss.js` | машина состояний мирового босса: щит из узлов, уязвимость, импульсы, перерождение |
| `pve-areas.js` | постоянные PvE-области: личные комнаты, выкат встреч по времени и пройденному пути, следы |
| `public-events.js` | временные публичные события: зона на карте, срок жизни, спорный сундук, задержка возврата после смерти |
| `public-event-scenarios.js` | механики сценариев событий: опорные объекты, обозначенный удар, опасная земля |

### Базы, кланы и осады

| Файл | Назначение |
|---|---|
| `personal-bases.js` | схема и нормализация личной базы аккаунта |
| `base-construction.js` | размещение объектов базы: сетка, лимиты тира, пересечения, стоимость |
| `base-jobs.js` | очередь производства базы и экономия сырья жителями |
| `base-residents.js` | жители базы: наём, назначение, лояльность, бонусы |
| `kromka-clans.js` | кланы и клановые базы: состояние, права управления, занятие базы, модули |
| `kromka-clan-benefits.js` | недельные доходы владений, модификаторы крафта, особые заказы, маршрут и защищённый сбор |
| `siege-scheduler.js` | окна осад по расписанию UTC и id событий |
| `siege-qualification.js` | вызовы, требование 72 часов членства, регистрация и блокировка составов |
| `siege-resolution.js` | ход осады: реле → пролом → ядро, волны возрождения, итог и награды |

### Локальные активности

| Файл | Назначение |
|---|---|
| `world-activity-runtime.js` | жизненный цикл активностей комнаты и авторитетный прогресс |
| `world-activity-director.js` | режиссёр темпа: волны, замена потерянных целей, восстановление точек |
| `world-activity-layout.js` | пространственная схема столкновения: район целей, линии подхода |
| `world-activity-matchmaking.js` | серверный подбор событий и «Быстрая вылазка» |
| `player-activity-recovery.js` | планирование восстановления проваленных активностей игрока |
| `world-party-integrity.js` | контракт task/party, identity и целостность мировых групп, уникальность `characterId` |
| `onsite-party-formation.js` | формирование отряда на месте |

`retired-environment-models.js` — реестр снятых моделей окружения и снятие их
коллизий в данных локаций; его читают только инструменты `tools/`, сервер его
не импортирует.

## Авторские и runtime-данные

| Путь | Содержимое |
|---|---|
| `data/locations/*.json` | авторские локальные карты и объекты |
| `data/global-map.json` | сетка, узлы, инфраструктура, объекты и encounter weights |
| `data/encounters.json` | состав шаблонных встреч |
| `data/quests.json` | квесты NPC |
| `data/traders.json` | торговые профили |
| `data/loot-tables.json` | таблицы контейнеров и врагов |
| `data/npc-routines.json` | авторские поведения NPC |
| `data/economy-recipes.json` | рецепты производства фракционной экономики |
| `data/anomalies.json`, `data/artifacts.json`, `data/mutants.json`, `data/base-residents.json` | каталоги Искажений, артефактов, существ и жителей базы |
| `data/kromka/*.json` | канон «Кромки»: прогрессия персонажа, предметы и полевые рецепты, экономика v3, A-Life, территория, PvE-области, публичные события, квесты, обучение, клановые базы, осады, строительство базы, фракции, локации, NPC, настройки симуляции |
| `data/generated/kromka/` | экспорт авторских сцен Unity: `world.json` и `save-migration.json` |
| `DATA_DIR/users.json` | аккаунты, email и сессии |
| `DATA_DIR/saves.json` | персонажи по user id и общее состояние сервера: счета аккаунтов (марки и синь), личные базы, кланы, осады, аванпосты, книги ордеров, Чёрный рынок, участки станков, публичные события, боссы, рождения артефактов |
| `DATA_DIR/wasteland-sim.json` | runtime-состояние живой пустоши |
| `DATA_DIR/danger-ecology.json` | логова и группы A-Life |
| `DATA_DIR/.kromka-state-transaction.json` | журнал записи `users.json`/`saves.json`; существует только во время транзакции |

Если `DATA_DIR` не задан, используется каталог `data/` в рабочей копии.
Runtime-файлы игнорируются Git. Авторские JSON читаются из поставки, даже если
в `DATA_DIR` их копий нет; глобальная карта из `DATA_DIR` дополняется строками
поставки с новыми идентификаторами (`global-map-merge.js`).

Запись выполняется через временный файл и rename. `users.json` и `saves.json`
пишутся транзакцией `kromka-state-store.js`: снимок сначала попадает в журнал,
затем в файлы, и незавершённая транзакция доигрывается при старте. Частые
session-touch обновления `users.json` объединяются в окно до секунды,
остальные изменения аккаунтов и сохранений записываются немедленно. При ошибке
чтения runtime JSON исходник сохраняется рядом как `*.corrupt-<timestamp>`.

## HTTP API

Основные публичные группы:

- `/health`;
- `/api/auth/*`;
- `/api/characters*`;
- `/api/locations`, `/api/quests`, `/api/global-map`, `/api/wasteland`;
- `/api/kromka/character-progression`, `/api/kromka/items`,
  `/api/kromka/artifacts` — публичные каталоги; Unity читает первые два;
- `/socket.io/`.

Маршруты `/api/dev/*` по умолчанию закрыты. Среди них: чтение и запись
локаций и глобальной карты для dev-редакторов, снимок, сброс и точки живой
пустоши, сводка A-Life (`GET /api/dev/danger-ecology`,
`POST /api/dev/danger-ecology/kill`) и `POST /api/dev/accounts/sin` — выдача
сини на счёт по `login` или `userId`, пока нет платёжного магазина. Режимы
доступа:

- `DEV_API_MODE=disabled` возвращает `404`;
- `DEV_API_MODE=local` разрешён только вне production, только для прямого
  loopback с локальными Host/Origin, заголовком `X-Dev-Local: 1` и без
  `Forwarded`, `X-Forwarded-*` и `X-Real-IP`;
- `DEV_API_MODE=token` требует `DEV_ADMIN_TOKEN` длиной не менее 32 UTF-8 байт
  и заголовок `X-Dev-Token`;
- `POST`, `PUT`, `PATCH` и `DELETE` dev API принимают только
  `Content-Type: application/json`.

HTML-редакторы `public/dev-location-editor.html` и
`public/dev-global-map-editor.html` сервер отдаёт только при включённом dev API
вне production.

Production Nginx отдельно возвращает `404` для dev API и HTML-редакторов до
общего `/api/` и static routing, включая варианты регистра и URL-кодирования.
Для административного token-доступа нужен непубличный прямой канал, например
SSH-туннель; штатный public Nginx его не пропускает.

Подробнее об auth и персонажах:
[Аккаунты и персонажи](AUTH_AND_CHARACTERS.md).

## Генераторы и проверки

- Статическое окружение локаций оформляется в Unity-сценах; серверу от него
  нужны только тайловая карта и коллизии объектов из `data/locations/*.json`.
- `tools/build-model-colliders.js` пересобирает actor collider-каталог
  `public/assets/models/wasteland/model-colliders.json` (персонажи и существа).
- `tools/optimize-glb.js` собирает облегчённые GLB в
  `public/assets/models-lite/` (`npm run build:models-lite`).
- `tools/build-global-map-playable.js` выгружает играбельный контур глобальной
  карты в `data/kromka/global-map-playable.json`.
- `tools/account-admin.js` обновляет email/пароль runtime-аккаунта
  (`npm run admin:account`).
- `tools/serve-unity-assets.js` — loopback-раздача `public/` для редакторских
  проб Unity без запуска игрового сервера.
- `tools/check-*.js` проверяют код, контент и синхронизационные контракты.
- `tools/smoke-check.js` запускает изолированный end-to-end smoke.

Основные команды:

```bash
npm run check:server
npm run check:dev-access
npm run check:world-party
npm run check:data
npm run check:npc
npm run check:economy
npm run smoke
npm run check
```

Smoke использует временный порт и временный `DATA_DIR`. Среди прочего он
проверяет версию `/health`, cache headers, auth lifecycle, блокировку
персонажа, создание/удаление персонажа, Socket.IO и совместную комнату трёх
игроков.

## Куда выносить следующий код

При дальнейшем разбиении `server.js` приоритетны границы с минимальным скрытым
состоянием: auth, персонажи, комнаты, бой, инвентарь и обработчики Socket.IO.

Перенос должен быть поэтапным: сначала чистая функция/подсистема и тесты, затем
подключение из `server.js`, затем удаление дубликата.
